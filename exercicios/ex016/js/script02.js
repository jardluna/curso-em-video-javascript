function soma(n1 = 0, n2 = 0) { //Quando não há chamada de um parâmetro, podemos pré-definir ele no return 
    return n1 + n2
}
//let res = Number(soma(2, 5))
//console.log(res) //Chamando a funcão com váriavel

console.log(soma(5, 2)) //Chamando a funcão sem váriavel